<Window x:Class="Elexim.Employee.ExpenseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Elexim.Employee"
        mc:Ignorable="d"
        Title="ExpenseView" Height="361" Width="423">


    <Grid  Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock HorizontalAlignment="Center">Dépenses</TextBlock>
            <Separator Margin="0, 10" />
        </StackPanel>


        <DataGrid Grid.Row="1">

        </DataGrid>

        <WrapPanel HorizontalAlignment="Right" Grid.Row="2" Orientation="Horizontal">
            <Button Content="Ajouter" Margin="5,0,0,0"/>
            <Button Content="Modifier"  Margin="5,0,0,0" />
            <Button Content="Supprimer"  Margin="5,0,0,0"/>
        </WrapPanel>
    </Grid>

</Window>
